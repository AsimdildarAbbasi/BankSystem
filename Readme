in this we are going to crea et a bank system backedn 
register login 
email send to ogin person who juts login

first 
user schema  in models 
email name password ycrypt using bcrypt function 

now first find one using enmail if eist error if not 
then create a user using(UserModel.create)
and then compare password 

/Now for the authetication we 
first create a user using signin route then 

login Route 

next task is to send a email to the remail by which use ris register(now we use nodemailer)
use google ai then gmaail api 

donee email by nodemailer
now we use accounts 
we have to make a account schema which have user by which account is created 
user status currency and index we use to find when the number of user increasae

now we use leager 
lets talk about what us ledger it keeps record of trasaction payemntts and evything related 
ledger wants 
source destination 
ammout 
idempotency key which is geerated at once and never used after teh transacin complete(
    same reuqest never be eecuted by two time like 
    if  some server slow down no transaction repeated 
)
Staus we store panding at the satrt

what is the ledger it is srt of  regitser stores  information about user account when data how much and from where
legder for A user
{
account A
ammount 100
type debit
}

ledger for destinaton
{
account user b 
ammount 100
type crdit }
now we chnage the status to make comlete
if one of the following thigs failed nothing will happer
now we have write the ledger scheeema for our transaction now we want to do wirte a bussniess logic

we have created a controller in which we have
his controller handles the creation of transactions and the associated ledger entries.
 * validate reques
 * validate idempotency key
 * creeate accuntstatus
 now we want to derive account balnace from source 